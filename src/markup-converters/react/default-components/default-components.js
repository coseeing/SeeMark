import { SUPPORTED_COMPONENT_TYPES } from '../../../shared/supported-components';

import Alert from './Alert';

const defaultComponents = { [SUPPORTED_COMPONENT_TYPES.ALERT]: Alert };

export default defaultComponents;
